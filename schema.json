{
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Oxide project where instances will be created."
    },
    "cpus": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of vCPUs to assign to the instance."
    },
    "memory": {
      "type": "integer",
      "description": "Amount of memory to assign to the instance, in bytes."
    },
    "disk_size": {
      "type": "integer",
      "description": "Disk size in bytes."
    }
  },
  "required": [
    "project",
    "cpus",
    "memory",
    "disk_size"
  ]
}
